<!DOCTYPE html>
<html>
  <head>
    <title>Bookingappv2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

<body>
    <main>
      <nav class="bg-white shadow-md" data-controller="mobile-menu">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between h-16">
            <div class="flex items-center space-x-4">
              <%= link_to root_path do %>
                <%= image_tag 'ventus_white.png', alt: 'Ventus Logo', class: 'h-12 w-12'  %>
              <% end %>
            </div>
            <div class="hidden md:flex space-x-8">
              <%= link_to 'Home', root_path, class: 'text-gray-900 inline-flex items-center px-1 pt-1 border-b-2 border-indigo-500 text-sm font-medium' %>
              <%= link_to 'Bookings', bookings_path, class: 'text-gray-500 hover:text-gray-700 inline-flex items-center px-1 pt-1 border-b-2 border-transparent text-sm font-medium' %>
            </div>

            <div class="flex items-center space-x-8">
              <div class="hidden md:flex items-center space-x-4">
                <% if user_signed_in? %>
                  <%= button_to 'Logout', destroy_user_session_path, method: :delete, class: 'text-gray-900 hover:text-gray-700' %>
                <% else %>
                  <%= link_to 'Sign In', new_user_session_path, class: 'text-gray-500 hover:text-gray-700 inline-flex items-center px-1 pt-1 border-b-2 border-transparent text-sm font-medium' %>
                  <%= link_to 'Sign Up', new_user_registration_path, class: 'text-gray-900 hover:text-gray-700 px-4 py-2 rounded-md bg-indigo-500 text-white font-medium' %>
                <% end %>
              </div>
            </div>

            <div class="md:hidden flex items-center">
              <button data-action="mobile-menu#toggle" class="text-gray-800 hover:text-gray-600 focus:outline-none">
                <svg class="h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7" />
                </svg>
              </button>
            </div>

          </div>
        </div>

        <div data-mobile-menu-target="menu" class="hidden md:hidden">
          <div class="px-2 pt-2 pb-3 space-y-1 sm:px-3">
            <%= link_to 'Home', root_path, class: 'block text-gray-900 px-3 py-2 rounded-md text-base font-medium' %>
            <%= link_to 'Bookings', bookings_path, class: 'block text-gray-500 hover:text-gray-700 px-3 py-2 rounded-md text-base font-medium' %>
          </div>
        </div>
      </nav>

      <div class="container mx-auto mt-4">
        <% if notice %>
          <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative" role="alert">
            <strong class="font-bold">Notice: </strong>
            <span class="block sm:inline"><%= notice %></span>
          </div>
        <% end %>
        <% if alert %>
          <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative" role="alert">
            <strong class="font-bold">Alert: </strong>
            <span class="block sm:inline"><%= alert %></span>
          </div>
        <% end %>
        <%= yield %>
      </div>
    </main>
  </body>
</html>
