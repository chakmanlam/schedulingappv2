<%= month_calendar events: bookings do |date, bookings| %>
  <%= date %>
  <% bookings.each do |booking| %>
    <div>
      <%= link_to booking.client.name, booking, class: "text-indigo" %>
    </div>
  <% end %>
<% end %>
